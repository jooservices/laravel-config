# Setup

## Install dependencies

```bash
composer install
```

## Provide MongoDB for tests

Set `MONGODB_URI` and `MONGODB_DATABASE` locally, or run a local MongoDB instance on `mongodb://localhost:27017`.

## Useful commands

```bash
composer lint
composer lint:all
composer test
composer check
```
